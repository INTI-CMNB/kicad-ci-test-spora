name: "KiCad CI/CD for Spora main board"

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    paths:
      - 'pcb_main/*.sch'
      - 'pcb_main/*.kicad_pcb'
      - 'pcb_main/*.kibot.yaml'
      - 'pcb_main/Makefile'
      - '.github/workflows/spora_main.yml'
  pull_request:
    paths:
      - 'pcb_main/*.sch'
      - 'pcb_main/*.kicad_pcb'
      - 'pcb_main/*.kibot.yaml'
      - 'pcb_main/Makefile'
      - '.github/workflows/spora_main.yml'

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  Fabrication:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: INTI-CMNB/KiBot@v1
      with:
        # Required - kibot config file
        config: pcb_main/spora.kibot.yaml
        # optional - prefix to output defined in config
        dir: pcb_main/Fabrication
        # optional - schematic file
        schema: 'pcb_main/spora.sch'
        # optional - PCB design file
        board: 'pcb_main/spora.kicad_pcb'
    - name: Retrieve results
      if: ${{ always() }}
      uses: actions/upload-artifact@v2
      with:
        name: Fabrication_Output
        path: |
          pcb_main/Fabrication/
        if-no-files-found: warn

